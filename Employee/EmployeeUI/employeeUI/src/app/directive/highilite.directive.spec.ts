import { HighiliteDirective } from './highilite.directive';

describe('HighiliteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighiliteDirective();
    expect(directive).toBeTruthy();
  });
});
